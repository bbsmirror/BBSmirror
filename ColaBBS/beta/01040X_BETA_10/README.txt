
  ColaBBS 01040X BETA 10

  一、新增與修正功能:
    請參考 Version.txt

  二、升級:
    請將 bin 目錄下所有的檔案複製到 BBS 的 bin 目錄中.
    
  三、其他
    新增下面兩個設定:
    disableDebugLog = true|false    是否關閉 debug 的 log. 預設值為 false.
    autoCheckCloak = true|false     是否在無管理者權限的使用者登入時, 將其隱身的狀態解除. 預設值為 false.

                                                                                     WilliamWey 2002/01/03
                                                                                   is85012@cis.nctu.edu.tw
